spring:
  profiles:
    active:
    - dev

---
spring:
  profiles: dev
  application:
    name: microservicecloud-config-slccys-dev

---
spring:
  profiles: test
  application:
    name: microservicecloud-config-slccys-test
